spring.application.name=starter-preflight-smoke
server.port=${SMOKE_SERVER_PORT:18080}

observation.heartbeat.portal-base-url=${OBSERVATION_PORTAL_BASE_URL:http://localhost:8080}
observation.heartbeat.project-key=${OBSERVATION_PROJECT_KEY:issued-project-key}
observation.metric-flush.environment=preflight
