# Observation Portal Frontend

Vite SPA workspace for the Observation Portal dashboard.

## Running The Code

Run `npm install` to install dependencies.

Run `npm run dev` to start the development server.

Run `npm run typecheck` to validate the TypeScript project configuration.
