# Guard Result - Story 14.2

## Commands

| Command | Result |
|---|---|
| `cd frontend && npm run guard:read-model-contract` | pass: `read-model contract guard fixtures passed` |
| `cd frontend && npm run typecheck` | pass |
| `cd frontend && npm run build` | pass: Vite build completed in `1.22s` |

## Static Grep Classification

| Search | Result |
|---|---|
| forbidden source/recalculation terms | hits are guard negative fixtures/assertions, source semantics comments, or marker-as-index explanatory comments. No new production user-facing fallback, raw explorer, health score, root cause, recovery proof UI was added. |
| `.sort(` / `.toSorted(` / `.reduce(` | no hits in `frontend/src/app/components`, `frontend/src/app/lib`, or `frontend/scripts`. |
| source semantics terms | expected hits in types, fixtures, guard, existing snapshot/instance surfaces, and new Dashboard context copy for `baseline not used`. |
| `Tabs` in `dashboard.tsx` | no `Tabs`, `TabsList`, or `TabsTrigger` hit. `SnapshotHistoryPanel` remains in `DashboardMain` same-flow order. |

## Browser QA Summary

| Viewport | Result |
|---|---|
| desktop `1440x1000` | auth-blocked shell only; 3-column Project/Application/Main composition visible; no horizontal scroll |
| tablet `1024x900` | auth-blocked shell only; Project/Application rails in two columns and Main next row; no horizontal scroll |
| mobile `390x844` | auth-blocked shell only; Project -> Application -> Main stack; global nav overflow resolved, `bodyScrollWidth=390` |

Authenticated Project -> Application -> Dashboard -> Snapshot -> Instance -> retention expired browser path remains a known fixture/runbook gap and is not claimed as proven.
